name: Full Pipeline
on:
  schedule:
    - cron: '0 3 * * *'  # Каждую ночь в 3:00

jobs:
  full-pipeline:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Full ETL pipeline
      run: |
        python main.py --skip-mysql